services:
  # Web Service
  - type: web
    name: barangay-system
    env: node
    rootDir: server
    buildCommand: "npm install && npm run build"
    startCommand: "node dist/index.js"
    envVars:
      - key: NODE_ENV
        value: production
